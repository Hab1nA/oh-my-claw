# OpenClaw最小化实现协作指南

## 一、项目概述

### １.１　实现目标

本项目旨在以最小化实现为原则，构建一个简洁但功能完整的OpenClaw风格AI Agent框架。通过两人协作的方式，我们将从零开始实现OpenClaw的核心功能，建立起一个可运行的技术框架。

项目的核心目标是实现一个本地优先的AI Agent执行网关，能够通过通讯渠道接收用户指令，调用大语言模型进行推理，并通过标准化的工具调用机制执行实际任务，同时保持对话记忆和主动服务能力。

### １.２　架构总览

基于OpenClaw的技术文档，我们确定最小化实现需要包含以下核心组件：

```
┌────────────────────────────────────────────────────────────────┐
│                      Gateway（网关）                            │
│                   Node.js常驻进程                               │
│    消息路由 │ 会话管理 │ 上下文构建 │ 工具调度                   │
└────────────────────────────────────────────────────────────────┘
                              │
              ┌───────────────┼───────────────┐
              ▼               ▼               ▼
┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐
│ Channel Adapter  │ │   Agent Runtime  │ │  Skills Platform  │
│    渠道适配器    │ │   Agent运行时   │ │    技能平台       │
│  统一消息格式    │ │  ReAct推理循环  │ │   工具注册调用    │
└──────────────────┘ └──────────────────┘ └──────────────────┘
              │               │               │
              │               │               │
              ▼               ▼               ▼
┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐
│   配置文件层     │ │    LLM调用层     │ │   本地存储层      │
│  SOUL.md/IDENTITY│ │   模型抽象接口   │ │  Memory/Heartbeat│
└──────────────────┘ └──────────────────┘ └──────────────────┘
```

### １.３　任务分解原则

为了实现高效的两人员协作开发，我们遵循以下原则进行任务分解：

**独立性原则**：每个任务流应该尽可能独立开发，减少相互依赖。接口和约定一旦确定，两边可以并行开发而无需等待对方完成具体实现。

**接口先行原则**：在开始具体实现之前，双方首先协商并确定模块之间的接口定义。明确的接口是并行开发的基础保障。

**功能完整性原则**：分解后的两个任务流完成后，合并应能得到一个完整可运行的系统。不存在缺失的关键功能。

**工作负载平衡原则**：两个任务的复杂度应尽量均衡，确保两位开发者的工作量大致相当。

## 二、任务流分解

根据上述原则，我们将项目分解为两个可并行进行的任务流：任务流甲专注于核心运行时和工具执行系统，任务流乙专注于渠道接入和技能扩展系统。两个任务流在接口层汇合，通过预定义的接口协议进行通信。

### 任务流甲：核心运行时与工具系统

#### 任务流甲概述

任务流甲负责实现OpenClaw的核心运行时系统，包括Gateway的主进程架构、Agent Runtime的推理引擎、工具调用执行器、以及与LLM的交互逻辑。这是整个系统的「大脑」和「四肢」，决定了Agent是否能够正确理解用户意图并执行相应操作。

#### 核心职责

任务流甲的开发者在整个项目中承担以下核心职责：

负责设计和实现Gateway主进程，包括进程启动、服务初始化、配置加载、以及优雅关闭机制。Gateway需要能够以守护进程的方式运行，处理各种信号，实现日志记录和错误处理。

负责实现Agent Runtime，这是系统的推理核心。Agent Runtime需要实现完整的ReAct循环，包括Prompt构建、LLM调用、响应解析、工具调用决策、以及循环终止条件判断。

负责实现工具调用执行器，这是系统执行实际操作的组件。工具执行器需要支持Shell命令执行、文件读写等基础操作，并提供标准化的工具注册和调用接口，供Skills系统扩展使用。

负责实现与LLM的交互逻辑，包括模型抽象层、请求构建、响应解析、错误处理、以及重试机制。需要支持主流的OpenAI兼容API格式。

负责实现基础的上下文管理，包括会话状态维护、对话历史管理、以及上下文窗口管理。

#### 技术要求

任务流甲需要使用Node.js作为运行时环境，采用TypeScript进行类型安全的开发。建议使用Node.js 22或更高版本，以获得更好的性能和最新的语言特性。

模块之间通过清晰的接口进行通信，不直接依赖其他模块的具体实现。所有对外暴露的接口应编写完整的TypeScript类型定义。

日志系统应支持多级别输出（debug、info、warn、error），并支持结构化日志格式，便于问题排查和系统监控。

错误处理应遵循一致的模式，所有异步操作使用Promise或async/await，并妥善处理各种异常情况。

#### 具体实现内容

**Gateway主进程**

Gateway主进程需要实现以下功能模块：

进程管理模块负责处理命令行参数解析、服务启动和停止、进程信号处理（SIGTERM、SIGINT等）、以及守护进程模式的实现。

配置加载模块负责从配置文件（YAML或JSON格式）读取系统配置，支持环境变量覆盖，并提供配置验证和默认值处理。

服务初始化模块负责按正确顺序初始化各个子系统，包括日志系统、数据库连接（如果有）、消息总线等依赖关系较少的组件优先初始化。

HTTP/WebSocket服务模块负责提供控制接口，默认监听18789端口。WebSocket用于实时消息推送，HTTP用于管理接口和健康检查。

**Agent Runtime**

Agent Runtime需要实现以下核心功能：

Prompt构建器负责根据当前上下文动态生成发送给LLM的提示词。提示词应包含系统角色定义、用户偏好信息、可用工具描述、会话历史摘要、以及当前任务上下文。Prompt构建器需要支持上下文长度控制，避免超出模型的token限制。

ReAct循环引擎负责实现完整的推理循环。引擎需要追踪当前状态，决定是生成回答还是调用工具，处理工具调用的结果，并判断是否已达到任务目标或需要终止循环。

模型调用器负责与LLM API交互。这包括构建符合API规范的请求、发送HTTP请求、解析响应、处理流式输出（可选）、以及实现重试和降级逻辑。

**工具调用系统**

工具调用系统需要实现以下功能：

工具注册表负责管理系统可用的所有工具。每个工具需要注册名称、描述、参数模式、以及处理函数。工具注册表提供按名称查询和按功能分类查询的接口。

参数验证器负责根据工具的schema验证传入的参数。这包括类型检查、必填字段检查、以及自定义验证规则。

执行器负责在安全的环境中执行工具调用。需要实现超时控制、错误捕获、以及执行结果序列化。

内置工具需要实现以下核心工具：

文件读取工具：读取指定路径的文件内容，支持编码指定和行范围限制。

文件写入工具：向指定路径写入内容，支持创建目录和文件备份。

Shell执行工具：在系统Shell中执行命令，支持超时控制和输出捕获，需要实现危险命令过滤。

HTTP请求工具：发送HTTP请求调用外部API，支持GET、POST等方法，支持自定义请求头和请求体。

**上下文与会话管理**

上下文管理器负责维护Agent执行过程中的状态信息。这包括当前对话的历史消息、工具调用的结果、以及各种临时状态。上下文管理器需要支持截断机制，当历史过长时保留最近的对话和最重要的上下文。

会话管理器负责管理系统中的所有会话。这包括创建新会话、恢复已有会话、清理过期会话、以及会话数据的持久化。

#### 验收标准

任务流甲完成后，应满足以下验收标准：

Gateway能够成功启动和停止，支持前台运行和守护进程模式，能够处理各种错误情况而不崩溃。

Agent Runtime能够接收用户消息，生成符合预期的Prompt，并正确解析LLM的响应。

ReAct循环能够正确处理单轮对话和多轮对话，能够识别并执行工具调用，能够正确终止循环。

工具调用系统能够注册和调用各种工具，能够验证参数和处理错误，能够实现超时控制。

系统能够与至少一个主流LLM API进行交互，能够处理网络错误和API错误。

### 任务流乙：渠道接入与技能系统

#### 任务流乙概述

任务流乙负责实现OpenClaw的外部交互层和扩展系统，包括Channel Adapters渠道适配器、Skills技能平台、以及用户配置和身份系统。这是整个系统的「感官」和「技能库」，决定了用户如何与Agent交互以及Agent能够执行哪些具体操作。

#### 核心职责

任务流乙的开发者在整个项目中承担以下核心职责：

负责设计和实现Channel Adapter框架，包括通用消息格式定义、渠道连接管理、消息收发处理、以及多渠道并发支持。

负责实现至少一个渠道适配器作为示例，建议选择Telegram，因其API相对简单且易于测试。后续可以扩展支持更多渠道。

负责实现Skills技能平台，包括技能加载机制、Skill文件格式定义、以及技能执行接口。需要实现几个基础的示例技能来演示功能。

负责实现用户配置系统，包括SOUL.md、IDENTITY.md、USER.md等配置文件的解析和应用。这些文件决定了Agent的行为方式和与用户的交互风格。

负责实现Heartbeat调度器，这是系统主动服务能力的基础。Heartbeat允许配置定时任务，使Agent能够在特定时间自动执行操作。

#### 技术要求

任务流乙同样使用Node.js和TypeScript开发，与任务流甲保持技术栈一致。

Channel Adapter需要实现标准化的接口规范，确保新渠道的添加不会影响核心系统和其他渠道的正常运行。

技能格式应采用Markdown为主的格式，便于人类阅读和维护，同时提供必要的元数据供系统解析。

配置文件采用人类可读的格式，如Markdown和YAML，便于用户直接编辑和理解Agent的配置。

#### 具体实现内容

**Channel Adapter框架**

Channel Adapter框架需要实现以下功能：

消息标准化模块负责定义OpenClaw内部统一的消息格式。这个格式需要包含消息ID、发送者信息、接收者信息、内容（支持文本和富媒体）、时间戳、以及附件信息。消息标准化是支持多渠道的基础。

适配器基类为每种具体渠道的适配器提供统一的父类。基类定义init（初始化连接）、send（发送消息）、receive（接收消息）、stop（停止连接）等抽象方法。适配器基类还应提供错误处理和重连机制。

消息路由负责将接收到的消息分发给对应的会话处理逻辑。当收到新消息时，路由需要确定消息所属的会话，并触发相应的处理流程。

多渠道协调确保来自同一用户的不同渠道的消息能够正确关联，同一会话可以跨越多个渠道保持上下文连续性。

**Telegram适配器实现**

作为示例渠道，Telegram适配器需要实现以下功能：

Webhook接收模式允许Telegram通过Webhook将消息推送到Gateway。这需要配置URL并处理Telegram的签名验证。

轮询接收模式作为Webhook的替代方案，通过定期调用Telegram API获取更新。

消息发送功能将Agent的响应发送回Telegram聊天。这包括文本消息、Markdown格式消息、以及图片和文件支持。

命令处理支持Telegram机器人的命令机制，如/start、/help等内置命令，以及用户自定义命令。

键盘和按钮支持Telegram的内联键盘和回复键盘，为用户提供交互式操作选项。

**Skills技能平台**

技能平台需要实现以下功能：

技能加载器负责扫描skills目录，加载所有技能模块，并注册到工具系统。每个技能目录应包含README.md描述文件、实现代码、以及可选的配置文件。

技能格式规范定义每个技能的结构：

```
skills/
└── example-skill/
    ├── README.md        # 技能描述和使用说明
    ├── skill.yaml       # 元数据配置
    └── index.ts/js      # 技能实现代码
```

技能执行接口定义技能与系统交互的方式。技能可以声明自己需要的工具、接收的参数、以及返回的结果格式。

示例技能需要实现以下基础技能作为参考：

天气查询技能：接收城市名称，返回天气预报信息。

提醒技能：设置提醒时间，到期时发送通知。

网页搜索技能：搜索互联网并返回相关结果摘要。

**用户配置系统**

用户配置系统需要解析和应用以下配置文件：

SOUL.md是Agent的核心行为定义文件，使用自然语言描述Agent的价值观、行为准则和工作方式。文件内容会被注入到系统Prompt中，影响Agent的回复风格。

IDENTITY.md定义Agent的身份特征，包括名称、年龄、背景故事、性格特点等。这些信息帮助塑造Agent的独特个性。

USER.md存储用户的个人信息和偏好，包括用户的姓名、工作背景、常用语言、偏好设置等。这些信息帮助Agent提供更个性化的服务。

**Heartbeat调度器**

Heartbeat调度器需要实现以下功能：

任务配置解析读取HEARTBEAT.md文件，解析其中的任务定义。每个任务包含触发条件（使用cron表达式）、任务描述、以及关联的技能或直接的操作指令。

定时执行器根据cron表达式计算下次执行时间，在指定时刻触发任务。这需要处理服务器时区和夏令时问题。

任务队列管理管理多个定时任务的执行，包括并发控制、错误处理和重试机制。

任务结果通知将任务执行结果通过配置的渠道通知给用户。

#### 验收标准

任务流乙完成后，应满足以下验收标准：

Channel Adapter框架能够正常工作，能够接收和发送消息，能够处理多种消息类型。

Telegram适配器能够成功连接到Telegram Bot API，能够接收用户消息并发送回复。

Skills平台能够加载和注册技能，能够通过自然语言触发技能执行，能够正确传递参数和处理结果。

配置文件能够被正确解析，能够影响Agent的行为和响应。

Heartbeat能够正确执行定时任务，能够发送任务结果通知。

## 三、模块接口定义

### ３.１　Gateway与Agent Runtime的接口

Gateway通过以下接口与Agent Runtime交互：

```typescript
interface AgentRuntime {
  // 处理用户消息，返回Agent响应
  processMessage(sessionId: string, message: Message): Promise<Response>;
  
  // 获取当前会话状态
  getSessionState(sessionId: string): Promise<SessionState>;
  
  // 终止正在进行的会话
  abortSession(sessionId: string): Promise<void>;
}
```

### ３.２　Agent Runtime与工具系统的接口

Agent Runtime通过以下接口与工具系统交互：

```typescript
interface ToolRegistry {
  // 注册新工具
  registerTool(tool: ToolDefinition): void;
  
  // 根据名称获取工具
  getTool(name: string): ToolDefinition | undefined;
  
  // 执行工具调用
  executeTool(name: string, params: Record<string, unknown>): Promise<ToolResult>;
  
  // 获取所有可用工具列表
  listTools(): ToolDefinition[];
}
```

### ３.３　Gateway与Channel Adapter的接口

Gateway通过以下接口与Channel Adapter交互：

```typescript
interface ChannelAdapter {
  // 初始化渠道连接
  init(config: ChannelConfig): Promise<void>;
  
  // 启动接收消息
  start(): Promise<void>;
  
  // 停止接收消息
  stop(): Promise<void>;
  
  // 发送消息到渠道
  send(destination: string, message: OutboundMessage): Promise<void>;
}

// Gateway通过事件接收来自适配器的消息
interface MessageHandler {
  onMessage(message: NormalizedMessage): void;
  onError(error: Error): void;
}
```

### ３.４　配置文件的接口

```typescript
interface SoulConfig {
  values: string;        // SOUL.md的内容
  behavior: string;      // 行为准则
}

interface IdentityConfig {
  name: string;
  background: string;
  personality: string[];
}

interface UserPreferences {
  name: string;
  language: string;
  timezone: string;
  channels: string[];
}
```

## 四、代码规范与约定

### ４.１　项目结构规范

项目采用以下目录结构组织代码：

```
openclaw-mini/
├── src/
│   ├── gateway/           # Gateway主进程
│   │   ├── index.ts       # 入口文件
│   │   ├── config/        # 配置管理
│   │   ├── services/      # 服务组件
│   │   └── utils/         # 工具函数
│   ├── agent/             # Agent Runtime
│   │   ├── runtime.ts     # 运行时核心
│   │   ├── react/         # ReAct实现
│   │   ├── prompt/        # Prompt构建
│   │   └── model/         # 模型交互
│   ├── tools/             # 工具系统
│   │   ├── registry.ts    # 工具注册表
│   │   ├── executor.ts    # 执行器
│   │   └── builtins/      # 内置工具
│   ├── channels/         # 渠道适配器
│   │   ├── adapter.ts     # 适配器基类
│   │   └── telegram/      # Telegram适配器
│   ├── skills/           # 技能平台
│   │   ├── loader.ts      # 技能加载器
│   │   └── runtime.ts     # 技能执行
│   ├── memory/           # 记忆系统
│   └── heartbeat/        # 定时调度
├── skills/               # 用户技能目录
├── memory/              # 记忆存储
├── config/              # 配置文件
│   ├── openclaw.yaml     # 主配置
│   ├── SOUL.md          # Agent灵魂
│   ├── IDENTITY.md      # Agent身份
│   ├── USER.md          # 用户配置
│   └── HEARTBEAT.md     # 定时任务
├── tests/               # 测试文件
└── package.json
```

### ４.２　命名规范

文件命名使用kebab-case格式，如message-handler.ts、tool-registry.ts。

类名使用PascalCase格式，如MessageHandler、ToolRegistry。

接口名以I开头或使用PascalCase，如IToolDefinition、ToolDefinition。

函数名使用camelCase格式，如processMessage、registerTool。

常量名使用UPPER_SNAKE_CASE格式，如MAX_TOKEN_LIMIT、DEFAULT_PORT。

类型和枚举成员同样使用PascalCase。

### ４.３　错误处理约定

所有异步函数必须使用try-catch或.catch处理错误，不允许未处理的Promise rejection。

自定义错误类应放在utils/errors.ts中，统一错误处理方式。

关键操作应记录详细日志，包括入参、操作步骤和错误信息。

用户可见的错误信息应友好且不暴露系统细节，技术细节只记录到日志。

### ４.４　日志规范

日志级别定义如下：debug用于开发调试信息，info用于正常运行信息，warn用于警告但可继续运行的情况，error用于错误和异常。

日志格式采用结构化格式，包含时间戳、级别、模块名和消息内容。

敏感信息如密钥、令牌等不能出现在日志中。

## 五、协作流程

### ５.１　开发前准备

在开始编码之前，两位开发者需要完成以下准备工作：

**接口确认会议**：共同审阅并确认模块接口定义，确保双方对接口的理解一致。任何接口变更都需要双方同意并更新文档。

**开发环境统一**：统一Node.js版本、IDE配置、ESLint和Prettier规则。创建统一的.gitignore和.editorconfig文件。

**分支策略**：使用Git进行版本控制，主分支为main，开发在dev分支上进行。各自的功能开发在对应的feature分支上进行，如feature/runtime-core、feature/channels-skills。

### ５.２　日常协作

**每日同步**：建议每日进行一次简短的同步会议或在线交流，分享进展和遇到的问题。

**代码审查**：合并到dev分支前需要相互审查代码，确保代码质量和一致性。

**接口变更**：如果接口需要变更，提前沟通并更新接口文档，必要时调整实现。

**问题上报**：遇到阻塞性问题时及时沟通，不要长时间卡在某个问题上。

### ５.３　集成与测试

**单元测试**：每个模块应编写对应的单元测试，确保核心功能的正确性。使用Jest或Vitest作为测试框架。

**集成测试**：当两个任务流各自完成核心功能后，进行集成测试，验证接口对接是否正常。

**端到端测试**：测试完整的消息流程，从渠道接收到最终响应返回。

### ５.４　文档维护

代码中的注释应清晰说明「为什么」而非「是什么」，重要逻辑需要添加注释。

README文件包含项目介绍、快速开始指南和配置说明。

接口文档记录所有公开接口的用法和示例。

## 六、里程碑计划

### 里程碑一：基础框架搭建（预计时间：1-2天）

任务流甲：完成Gateway主进程框架搭建，实现基本的HTTP/WebSocket服务。

任务流乙：完成Channel Adapter框架设计，实现Telegram适配器基础功能。

交付物：可运行的基础框架，能够启动Gateway并接收Telegram消息。

### 里程碑二：核心功能实现（预计时间：3-5天）

任务流甲：完成Agent Runtime实现，包括Prompt构建、ReAct循环、LLM调用、基础工具。

任务流乙：完成Skills平台基础功能，实现配置文件解析和Heartbeat调度器。

交付物：能够处理用户消息并执行简单工具调用的完整Agent。

### 里程碑三：功能完善与集成（预计时间：2-3天）

任务流甲：完善工具系统，增加更多内置工具，优化错误处理。

任务流乙：完善技能系统，实现示例技能，优化用户配置。

交付物：功能完善的最小化实现版本，可用于基本使用场景。

### 里程碑四：测试与优化（预计时间：1-2天）

联合进行完整的集成测试和端到端测试。

修复发现的问题，优化性能和稳定性。

编写用户文档和开发者文档。

交付物：可发布使用的稳定版本。

## 七、风险与对策

### 风险一：接口不一致

如果在开发过程中发现接口设计与实际需求不符，可能需要返工。

对策：在开始编码前充分讨论接口设计，进行原型验证。遇到问题时及时沟通，调整接口定义。

### 风险二：技术难度超出预期

某些功能如ReAct循环、工具执行沙箱等可能比预期复杂。

对策：采用渐进式开发，先实现简化版本，复杂功能后续迭代优化。遇到困难及时求助或调整方案。

### 风险三：LLM API兼容性问题

不同LLM提供商的API可能存在差异，需要适配处理。

对策：先以OpenAI API为主实现，确认基本流程后再扩展支持其他提供商。使用兼容层抽象API差异。

### 风险四：进度差异

两位开发者的工作进度可能不一致。

对策：设置明确的里程碑和验收标准，定期同步进度。如果一方落后，另一方可在完成自己任务后协助。

## 八、后续扩展方向

当前最小化实现完成后，可以考虑以下扩展方向：

支持更多渠道如WhatsApp、Slack、Discord等，实现真正的多渠道统一接入。

扩展技能库，从ClawHub社区获取或自行开发更多实用技能。

增加更多内置工具，如浏览器自动化、数据库操作、邮件管理等。

优化Agent的记忆系统，实现更智能的上下文管理和记忆检索。

增加安全机制，如操作确认、权限分级、审计日志等。

支持本地模型部署，如通过Ollama运行开源模型，实现完全离线的AI Agent。

## 九、联系方式与资源

本项目采用GitHub进行协作，代码仓库地址由团队自行搭建。

遇到问题或需要讨论时，可通过项目issue系统或即时通讯工具进行交流。

建议定期召开项目会议，保持信息同步和协作效率。

## 十、附录

### 附录A：技术栈参考

Node.js：版本22或更高。

TypeScript：严格模式启用，确保类型安全。

测试框架：Jest或Vitest。

日志库：Pino或Winston。

HTTP客户端：Axios（用于LLM API调用）。

配置文件：YAML（使用js-yaml解析）。

### 附录B：参考资源

OpenClaw官方GitHub仓库：github.com/openclaw/openclaw

OpenClaw官方文档：docs.openclaw.ai

ReAct论文：Synergizing Reasoning and Acting in Language Models

Cron表达式参考：可用于Heartbeat任务的时间配置
